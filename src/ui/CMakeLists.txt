set(UI_SOURCES
	win_widget.cpp
	new_win.cpp
	resize_canvas.cpp
)
set(UI_HEADERS
	win_widget.hpp
	new_win.hpp
	resize_canvas.hpp
)
add_library(ui-lib
	${UI_SOURCES}
	${UI_HEADERS}
)
target_link_libraries(ui-lib
	raylib
	raygui
)
